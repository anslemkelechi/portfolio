// Settings for mouse js
options = {
  cursorOuter: 'circle-basic',
  hoverEffect: 'pointer-blur',
  hoverItemMove: false,
  defaultCursor: false,
  outerWidth: 60,
  outerHeight: 60,
}
magicMouse(options)

// Initialize AOS
AOS.init()
